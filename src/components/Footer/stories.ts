import { Meta, StoryObj } from "@storybook/react"

import Footer from "."

export default {
  title: "Resgatemais/Footer",
  component: Footer,
  args: {},
  parameters: {
    layout: "fullscreen"
  }
} as Meta

export const Default: StoryObj = {}
