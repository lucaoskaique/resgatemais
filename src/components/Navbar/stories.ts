import type { Meta, StoryObj } from "@storybook/react"

import Navbar from "."

const meta: Meta = {
  title: "ResgateMais/Sections/Navbar",
  component: Navbar,
  parameters: {
    layout: "fullscreen"
  }
}

export default meta

export const Default: StoryObj = {}
