import { Meta, StoryObj } from "@storybook/react"

import { Header } from "."

export default {
  title: "Resgatemais/Header",
  component: Header,
  tags: ["autodocs"],
} as Meta

export const Default: StoryObj = {}
