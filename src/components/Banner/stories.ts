import { Meta, StoryObj } from "@storybook/react"

import { Banner } from "."

export default {
  title: "Banner",
  component: Banner,
  tags: ["autodocs"],
  parameters: {
    layout: "centered"
  }
} as Meta

export const Default: StoryObj = {}
